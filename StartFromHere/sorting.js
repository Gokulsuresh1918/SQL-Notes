//! Sorting

// This will sort the output in ascending order by the Country column.//default
SELECT *
FROM Customers
ORDER BY Country;

// To sort in descending order, you would add the DESC keyword:
SELECT *
FROM Customers
ORDER BY Country DESC;